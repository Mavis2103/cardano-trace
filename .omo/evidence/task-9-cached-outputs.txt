TASK: Fix B7 — cached_outputs parameter accepted by forward.py but never used

FIX:
In src/utxo_tracer/tracing/forward.py, added `elif` branch in the spent-refs
resolution logic (before the provider.get_spent_utxos call):

    if address in spent_cache:
        spent_refs = spent_cache[address]
    elif cached_outputs and address in cached_outputs:
        # Use cached output refs instead of calling provider
        spent_refs = [
            OutRef(tx_hash=node_id.rsplit(":", 1)[0],
                   output_index=int(node_id.rsplit(":", 1)[1]))
            for node_id in cached_outputs[address]
        ]
        spent_cache[address] = spent_refs
    else:
        try:
            spent_refs = await asyncio.wait_for(
                provider.get_spent_utxos(address), timeout=15.0)
            spent_cache[address] = spent_refs
        except ...

TEST:
tests/test_forward.py::test_cached_outputs_used — verifies that when
cached_outputs contains the target address, get_spent_utxos is NOT called
and the trace still produces correct steps.

RESULTS: All 11 tests pass (including test_backward and test_cache).
